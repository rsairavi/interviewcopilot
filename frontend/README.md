# Frontend (planned target)

This folder is reserved for aligning with SFMS-style structure.

Current frontend runtime is still at repository root:

- `src/app/*`
- `package.json`
- `playwright.config.ts`

When migrating, copy/move these into `frontend/` and update CI/CD workflows accordingly.
